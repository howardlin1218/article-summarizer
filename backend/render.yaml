# render.yaml
services:
  - type: web
    name: fastapi-backend
    env: python
    plan: free
    buildCommand: ""
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    workingDir: backend
    region: oregon
